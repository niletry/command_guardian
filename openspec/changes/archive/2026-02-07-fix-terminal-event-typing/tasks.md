## 1. Implement Types

- [x] 1.1 Define `TaskOutputPayload` interface in `TerminalView.tsx`.
- [x] 1.2 Update the `listen` callback in `TerminalView.tsx` to use the interface.

## 2. Verify

- [x] 2.1 Ensure the project compiles without errors.
